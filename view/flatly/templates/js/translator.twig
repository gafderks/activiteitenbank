{# The |dt filter is defined in \View\Extension\TranslateDummyTwigExtension #}
{% set dictionary = [
    "You need to supply a message"|dt,
    "Unable to save comment..."|dt,
    "Unable to save rating..."|dt,
    "Unspecified"|dt,
    "Minimum group size needs to be at least 1"|dt,
    "or more participants"|dt,
    "participants"|dt,
    "Maximum group size needs to larger than or equal to minimum group size"|dt,
    "Maximum group size needs to be at least 1"|dt,
    "en"|dt,
    "Drop files here to upload"|dt,
    "Your browser does not support drag'n'drop file uploads."|dt,
    "Please use the fallback form below to upload your files like in the olden days."|dt,
    "You can't upload files of this type."|dt,
    "File is too big ({{filesize}}MiB). Max filesize: {{maxFilesize}}MiB."|dt,
    "Server responded with {{statusCode}} code."|dt,
    "Cancel upload"|dt,
    "Are you sure you want to cancel this upload?"|dt,
    "Remove file"|dt,
    "You can not upload any more files."|dt,
    "Saving failed... Are you still connected?"|dt,
    "Saving failed..."|dt,
    "No data available in table"|dt,
    "Showing _START_ to _END_ of _TOTAL_ entries"|dt,
    "Showing 0 to 0 of 0 entries"|dt,
    "(filtered from _MAX_ total entries)"|dt,
    "Show _MENU_ entries"|dt,
    "Loading..."|dt,
    "Processing..."|dt,
    "Search"|dt,
    "No matching records found"|dt,
    "First"|dt,
    "Last"|dt,
    "Next"|dt,
    "Previous"|dt,
    "activate to sort column ascending"|dt,
    "activate to sort column descending"|dt,
    "Change columns"|dt,
    "Worst"|dt,
    "Bad"|dt,
    "Weak"|dt,
    "Good"|dt,
    "Strong"|dt,
    "Strength:"|dt,
    "Use a few words, avoid common phrases"|dt,
    "No need for symbols, digits, or uppercase letters"|dt,
    "Add another word or two. Uncommon words are better."|dt,
    "Straight rows of keys are easy to guess"|dt,
    "Short keyboard patterns are easy to guess"|dt,
    "Use a longer keyboard pattern with more turns"|dt,
    "Repeats like 'aaa' are easy to guess"|dt,
    "Repeats like 'abcabcabc' are only slightly harder to guess than 'abc'"|dt,
    "Avoid repeated words and characters"|dt,
    "Sequences like abc or 6543 are easy to guess"|dt,
    "Avoid sequences"|dt,
    "Recent years are easy to guess"|dt,
    "Avoid recent years"|dt,
    "Avoid years that are associated with you"|dt,
    "Dates are often easy to guess"|dt,
    "Avoid dates and years that are associated with you"|dt,
    "This is similar to a commonly used password"|dt,
    "This is a top-10 common password"|dt,
    "This is a top-100 common password"|dt,
    "This is a very common password"|dt,
    "A word by itself is easy to guess"|dt,
    "Names and surnames by themselves are easy to guess"|dt,
    "Common names and surnames are easy to guess"|dt,
    "Capitalization doesn't help very much"|dt,
    "All-uppercase is almost as easy to guess as all-lowercase"|dt,
    "Reversed words aren't much harder to guess"|dt,
    "Predictable substitutions like '@' instead of 'a' don't help very much"|dt,
    "The passwords do not match."|dt,
    "The strength of the password is insufficient."|dt
  ] %}
/*global baseUrl*/

/**
 * This script handles the translation of strings for other scripts.
 * To add a string for translation, add it to the array in /view/{template}/templates/js/translator.twig
 *
 * Depends: jquery
 */
Translator = {

    dictionary: {{ '{' }}{% spaceless %}
        {% for entry in dictionary %}"{{ entry|raw }}":"{{ entry|trans|raw }}"{% if loop.last == false %},{% endif %}{% endfor %}
    {% endspaceless %}{{ '}' }},

    /**
     * Looks up the source in the dictionary.
     *
     * @param source
     * @returns {*}
     */
    translate: function(source) {
        "use strict";
        if (Translator.dictionary[source] !== undefined) {
            return Translator.dictionary[source];
        } else {
            return source;
        }
    }
};